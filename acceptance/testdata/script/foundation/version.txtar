# urfave/cli provides the version flag and output format.
exec tuck --version
stdout 'tuck version dev'

exec tuck -v
stdout 'tuck version dev'

exec tuck --version --json
stdout '"kind":"version"'
stdout '"version":"dev"'
